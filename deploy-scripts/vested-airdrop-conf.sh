#sponsors configuration file.
# "sourced" into the script, so all items should be exported

export VESTED_AIRDROP_OUT=$DEPLOY_DIR/vested-airdrop-out.sh

export EXPECTED_DISTRIBUTION_TOTAL_CHECKSUM='13760760'

# assuming only airdrop merkle distributor tokens are not transferred
export EXPECTED_EXCESS_TOKENS_AFTER_VESTED_AIRDROP='46239240'
